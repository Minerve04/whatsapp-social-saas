# --- Base de donnees ---
# Utilisee par les scripts db:* a la racine (et par le backend).
DATABASE_URL=postgres://saas:saas@localhost:5432/saas

# --- Ports (dev) ---
# Voir backend/.env et portal/.env.local pour les variables propres a chaque service.
